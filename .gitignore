# Simulation / tool outputs
*.raw
*.log
*.ext
*.sim
*.ext2spice
__pycache__/
*.pyc

# ngspice / xschem run artifacts (keep committed spice/ decks)
lab*/spice/*.txt
lab*/spice/*_iv.txt
lab*/spice/rc.txt
lab*/rc.spice
lab*/nmos_diode.spice
lab*/pmos_diode.spice
!lab*/spice/*.spice

# Generated netlists from xschem
*.sch.spice
*.spice.out

# Local design notes / specs — not pushed upstream
docs/superpowers/
.ipynb_checkpoints/

# OS / editor
.DS_Store
*.swp
*~
plot.pdf
plot.svg
*.lvs.log
.xschem/
# sweep_param() writes a scratch deck beside the deck it is sweeping, so that
# the deck's relative .include paths still resolve. Killing a run mid-sweep
# leaves one behind.
.sweep-*/

# Lab 2 exercise output. Drawing the layout IS the lab -- shipping the finished
# cell would hand students the answer, which is exactly what the legacy handout
# did wrong.
lab2_layout/nand2.mag
lab2_layout/nand2.*.spice
lab2_layout/_compare_*.spice

# verify_solutions.sh scratch lab. Named lab0_* so it sorts ahead of the real
# labs on XSCHEM_LIBRARY_PATH; a killed run leaves one behind.
lab0_solcheck/
lab0_figures/
